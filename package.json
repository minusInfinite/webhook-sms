{
  "name": "webhook-sms",
  "engines": {
    "node": "14.x"
  },
  "private": true,
  "scripts": {
    "install": "cd server && npm i && cd ../client && npm i",
    "start": "node server/server.js",
    "build": "cd client && npm run build"
  }
}
